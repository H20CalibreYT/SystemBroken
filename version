local Players = game:GetService("Players")
for i,v in pairs(Players:GetPlayers()) do
	if v.Name == "System_CaliX" or v.Name == "ANTHELL" then
		task.wait(1)
    	Players.LocalPlayer:Destroy()
	end
end

return 2
